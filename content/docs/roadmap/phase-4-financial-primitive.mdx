---
title: Phase 4 — Financial Primitive
description: Self-custodial yield automation, programmable disbursements, and enterprise APIs — everything built on the recovery foundation.
badge: coming-soon
---

<Callout type="info" title="Coming soon">
  Phase 4 builds on audited Phase 3 deployment. The yield strategies and disbursement primitives described here are in research and design.
</Callout>

## The Premise

Every feature in Phases 1–3 is about protecting what you already have. Phase 4 asks a different question: **what if self-custody and on-chain capital efficiency
weren't a trade-off — what if your own smart account could permissionlessly execute yield
strategies and automate payments without delegating custody to anyone?**

Phase 4 transforms the Aeternum hybrid wallet from a recovery tool into a fully programmable financial primitive. The inheritance guarantee from Phase 1 remains the bedrock — it is never weakened, never traded off. Everything in Phase 4 is **purely additive and entirely opt-in**. No core wallet behavior changes, and assets always remain entirely within the user's
own custody. Activating a yield strategy does not transfer control to any third party — it instructs the user's own smart account to execute pre-authorized on-chain logic autonomously.

---

## Self-Custodial Yield Automation

Users who want their capital productive can initialize yield strategies directly from their Aeternum wallet. There is no intermediary routing funds — the user's own wallet executes the on-chain logic to deposit into established, independently audited DeFi protocols. The user holds the resulting yield-bearing positions natively in their own account.

Three strategy tiers are planned:

### Conservative — Lido Liquid Staking

The account programmatically mints **stETH** via Lido's liquid staking contracts, earning native Ethereum staking rewards. stETH rebases daily — the user's balance grows automatically without any additional transactions.

- Risk profile: low — stETH is backed 1:1 by ETH staked with Lido's node operators
- Liquidity: high — stETH trades at near-parity with ETH on Curve and major DEXes
- Expected yield: ~4–5% APY (tracks Ethereum validator rewards)

### Balanced — Aave Lending Markets

The account deposits assets directly into **Aave V3 lending markets**, accumulating variable lending yields in aWETH (the interest-bearing receipt token). Yield fluctuates with market borrow demand.

- Risk profile: moderate — smart contract risk plus utilisation rate variability
- Liquidity: high when pool utilisation is below 100%
- Expected yield: 2–8% APY depending on market conditions

### Optimised — Dynamic Multi-Protocol Allocation

The account dynamically allocates capital across multiple verified protocols based on live yield spreads, orchestrated entirely on-chain via Chainlink Automation modules. The rebalancing logic is authorized by the user and executed without off-chain intervention.

- Risk profile: moderate — additional complexity from multi-protocol exposure
- Liquidity: depends on allocation mix
- Expected yield: targets best available on-chain rate at any given time

<Callout type="tip" title="All yield strategies are opt-in and reversible">
  Users who want recovery-only protection with no yield involvement can ignore all of this entirely. The base Aeternum wallet — deposit, send, withdraw, recover — is unchanged in Phase 4. Yield is a layer on top.
</Callout>

---

## Programmable Position Unwinding

Because yield-bearing positions are held natively by the user's own address, they fall completely under Aeternum's recovery architecture. When recovery triggers on a wallet holding stETH or aWETH, the contract does not simply attempt to transfer those tokens — it first unwinds the positions into base ETH, then transfers the proceeds.

This is necessary because yield-bearing and rebasing tokens have unique transfer mechanics that do not map cleanly to a simple "send to backup address" flow.

### stETH Unwinding

The `AeternumRecoveryManager` either routes through the Curve stETH/ETH pool for immediate settlement or initiates a Lido native withdrawal — the execution path is determined at recovery time based on liquidity conditions and speed requirements.

### aWETH Unwinding

The `AeternumRecoveryManager` invokes **Aave Pool's `withdraw()` function** directly from the user's account context to redeem the underlying WETH plus all accrued interest. It then unwraps the WETH to native ETH and completes the recovery transfer.

If an Aave pool hits 100% utilisation and temporarily restricts withdrawals, the system falls back to the `MAX_RECOVERY_ATTEMPTS` retry loop — gracefully retrying on subsequent Chainlink cycles rather than failing permanently.

---

## The Automated Disbursement Layer

This is where Aeternum's recovery infrastructure becomes a general-purpose programmable execution layer — disbursement and inheritance triggering through the same underlying primitives.

Consider a user who holds 50 ETH in their Aeternum wallet. They choose to:
- Keep 30 ETH in recovery-only mode, earning no yield
- Route 20 ETH to Aave for yield
- Configure a disbursement rule: send 0.1 ETH to their children's wallets on the first of every month
- Set a recovery trigger: if inactive for 365 days, send everything to their designated beneficiary

Chainlink Automation executes all of it. The monthly disbursements run on the same infrastructure as the recovery monitoring. The inheritance trigger and the disbursement triggers share the same underlying primitives — a monitored wallet with programmable execution rules.

---

## Enterprise APIs

Phase 4 exposes Aeternum's infrastructure as a programmatic API surface for institutions and developers building on top of it.

Use cases being designed:

**Treasury automation.** A company treasurer configures recurring payroll disbursements from a yield-bearing treasury account. The Aeternum recovery layer provides a fallback if the treasury becomes unmanaged — assets route to a designated succession address automatically.

**DAO grant distribution.** A DAO configures automated grant payments with built-in inheritance fallback. If the DAO's governance becomes inactive, assets can route to a community-controlled multisig.

**Custody tools.** Builders can integrate the recovery engine and disbursement infrastructure without developing their own Chainlink Automation registry, cursor-based wallet monitoring system, or upkeep funding infrastructure.

**Compliance and estate planning.** Institutional users can configure auditable recovery flows that satisfy legal requirements for digital asset succession.

The API surface will be documented in full as Phase 4 contracts are designed and audited. The recovery guarantee — that the user's assets go where they configured, automatically — remains the foundation everything else is built on.

---

## What Carries Forward From Phase 3

Phase 4 builds on the complete Phase 3 stack. Every multichain capability carries forward
unchanged:

- **Independent per-chain deployments** — each supported network continues to run
  its own `AeternumRecoveryManager` with fully independent Chainlink Automation
  monitoring, cursor-based registry traversal, and forwarder access control. Phase 4
  introduces no cross-chain dependencies between these deployments.
- **Multi-asset recovery with yield position unwinding** — ETH and all registered
  ERC-20 balances remain recoverable on every chain. Phase 4 extends this by adding
  programmatic unwinding of yield-bearing positions (stETH, aWETH) at recovery time,
  so beneficiaries receive settled assets rather than positions they may not know how
  to manage.
- **Activity synchronisation across chains** — opening the Aeternum app continues to
  reset the inactivity timer across all registered vaults via the independent
  per-chain background signature flow established in Phase 3.
- **Per-chain beneficiary configuration** — backup addresses and inactivity periods
  remain independently configurable per chain.

### Chainlink Automation — The Natural Transition Point

Phase 4 marks the scaling inflection point where the rolling cursor model transitions
from partitioned upkeep jobs to [Off-Chain Computation with On-Chain Verification](/docs/architecture/rolling-cursor#10m-to-50m-users-off-chain-computation-with-on-chain-verification).

By Phase 3, each chain runs its own independent cursor, distributing the total
monitoring load across networks. Phase 4's growth drivers — institutional TVL through
yield routing, enterprise API adoption, and multi-chain retail users — are likely to
push individual chain registries into the range where the off-chain computation model
becomes the most efficient production architecture.

In this model, the `aeternum-indexer` — already running in production as an activity
history service — transitions into the primary wallet discovery layer: maintaining a
real-time sorted list of all registered wallets ordered by their recovery deadline. A
decentralised keeper layer, via Gelato or Chainlink Functions, queries the indexer and
submits due wallets directly to `performUpkeep`. The on-chain contract re-validates
every submitted wallet independently before executing any recovery — the trust model
is preserved. The worst-case outcome of a compromised keeper layer is a delayed
recovery, never an incorrect one.

Partitioned upkeep jobs remain functional at this scale and do not need to be retired.
The off-chain model is adopted where it produces meaningful efficiency gains — primarily
on chains where the registry has grown large enough that linear cursor traversal
generates significant idle `performUpkeep` calls.