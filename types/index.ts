/**
 * types/index.ts
 *
 * Single entry point for all type imports across the project.
 * Import from "@/types" instead of "@/types/vault" directly.
 *
 * Usage:
 *   import type { RecoveryConfig, VaultStatus, TimeRange } from "@/types"
 */

export * from './vault'